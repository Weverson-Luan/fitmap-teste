/**
 * Stub para imports de `.css` (ex.: `src/shared/styles/global.css`, importado
 * pelo `App.tsx` pro NativeWind). O Metro processa esse CSS de verdade no
 * build; o Jest não sabe interpretar CSS, então só substitui o import por um
 * módulo vazio.
 */
module.exports = {};
